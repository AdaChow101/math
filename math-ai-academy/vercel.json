{
  "version": 2,
  "builds": [
    {
      "src": "backend/server/index.js",
      "use": "@vercel/node",
      "config": {
        "maxLambdaSize": "50mb",
        "supportsResponseStreaming": true
      }
    },
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "rewrites": [ 
    // 1. API 代理：将所有 /api/* 的请求重写到后端服务入口
    // Vercel 会自动将此路径解析为 Serverless Function
    {
      "source": "/api/(.*)",
      "destination": "/backend/server/index.js"
    },
    // 2. SPA 路由：这是关键的 SPA 历史记录回退规则
    // Vercel 会自动将 /frontend/dist 解析为静态构建产物
    // Vercel 默认会将静态构建的根路径识别为 /
    {
      "source": "/(.*)",
      "destination": "/index.html" 
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        },
        {
          "key": "Cache-Control",
          "value": "public, max-age=0, must-revalidate"
        }
      ]
    },
    {
      "source": "/assets/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ]
}
